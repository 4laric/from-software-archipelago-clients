# Agent and contributor rules

Read [CONTRIBUTING.md](CONTRIBUTING.md) first. This file holds the rules that
are easy to break without noticing.

## Client updates must keep old seeds working

Players are mid-run on seeds generated by an earlier apworld and launcher
release, and the standard upgrade path for them is to replace **only** the
client binary. A client change is therefore backwards compatible with every
seed the previous release accepted unless it is loudly flagged as not.

"Compatible" covers all of these at once:

- **Seed contract.** Slot data, `runtime_config.json`, and the runtime build
  string an older world emits still load. New fields are optional with a
  default; removed fields are tolerated, not refused.
- **Persisted state.** Existing ledgers, receipts, journals, parks, and
  watermarks parse and keep their meaning. No schema-version bump without a
  migration that runs on load.
- **Recovery.** If a newer rule has to refuse something an older client
  persisted, refuse it by *parking that one item* through the existing
  terminal-failure path so the stream advances, name the index in the
  diagnostic, and make sure an existing console command (`blocked`,
  `retry INDEX CONFIRM`, `reissue`) clears it. A bare error that re-raises on
  every poll is a stall, not a refusal. Worked example: the reinforced-shield
  park in `crates/bb-archipelago/src/client_loop.rs` (PR #641), which
  replaced a bail that had wedged every later item.
- **Launcher interface.** CLI flags and config keys the launcher writes keep
  working; new ones are optional.
- **Behavior parity.** If delivery outcomes change for an existing run (for
  example an upgrade-level census now sees more weapons), say so in the
  changelog even when nothing breaks.

## Version numbers: V.R.M.F

Every game client in this workspace that pairs with an apworld uses a
four-part version, **Version.Release.Modification.Fixpack**, and the rule is
one sentence: **a client is compatible with every seed generated on the same
V.R.M; only the fixpack differs between them.**

- **V.R.M** is the seed-compatibility line. Apworld and client share it. Any
  change to V, R, or M is a paired apworld-and-client release and may change
  the seed contract, persisted state, or delivery behavior; it is announced
  as described in the next section.
- **F, the fixpack.** The bar is one question: *can a player swap to the
  V.R.M.F client on a seed that was generated with any apworld on the same
  V.R.M?* Either side may change in a fixpack so long as the answer is yes --
  an apworld change that alters NEW seeds (a scaling recalibration, say) is
  fixpack-eligible, because a seed already rolled is untouched and the new
  client still plays it. What a fixpack must never do is strand a running
  seed: every seed and every ledger the F-1 client accepted, the F client
  accepts, with no player action beyond replacing the binary. Everything in
  the compatibility section above applies without exception.
- The apworld side spells the same number `0.6.0.1` (its `APWORLD_VERSION`,
  the seed's `versions` string, its changelog and ledger); this crate spells
  it `0.6.0+f1` because Cargo needs a semver core. `er_semver` parses both
  and `er_logic::version::release_form` maps the Cargo spelling to the
  apworld one. The apworld's `archipelago.json` carries only `0.6.0`:
  Archipelago unpacks `world_version` into a three-field tuple.

How the four parts map onto what the code already checks:

| game | V.R.M lives in | F lives in | what the seed gate compares |
| --- | --- | --- | --- |
| Elden Ring | `crates/eldenring-archipelago/Cargo.toml` `major.minor.patch` | Cargo build metadata, `0.5.8+f1` | the apworld's `versions` band via `er-semver`, which strips `+metadata`, so a fixpack never crosses a band boundary |
| Bloodborne | `crates/bb-archipelago/Cargo.toml` `major.minor.patch` and the apworld's `RUNTIME_BUILD` | Cargo build metadata, `0.1.0+f1` | `RUNTIME_BUILD`, exact match; a fixpack never touches it |

Rules that follow:

1. A pull request that bumps only F may not change `RUNTIME_BUILD` or any
   persisted schema. The ER contract hash MAY move on a fixpack **only** when
   the same PR teaches the client to accept the previous contract of that
   V.R.M line (`er_logic::client_features::is_legacy_contract_compatible`
   lists the audited pair) -- a `0.6.0.x` client must play every `0.6.0`
   seed. If it cannot, it is an M bump, not a fixpack.
2. Release tags and changelog headings use the full `V.R.M.F` string
   (`bb-0.1.0.1`, `er-0.5.8.1`), so players can read whether an update is a
   drop-in binary swap (same V.R.M) or a paired upgrade.
3. The first release under this policy is `.0`; existing releases are read as
   fixpack 0 of their V.R.M. Bloodborne's current `bb-0.1.0-r10` runtime
   build stays exactly as is until the next deliberate break, when it moves to
   the `bb-V.R.M` form.
4. Dark Souls III and Sekiro clients are not under this policy until they
   pair with an apworld that publishes a matching V.R.M; note that in the
   PR when they do.

## When breaking compatibility is the right call

Sometimes it is. Then it must be loud, in all of these places:

1. Bump the cross-repo runtime build string (`RUNTIME_BUILD` in
   `crates/bb-archipelago/src/lib.rs`) together with the apworld's copy, so
   the client refuses old seeds up front with a clear message instead of
   misbehaving later.
2. Put a **Breaking** section at the top of the crate changelog that says
   what stops working and what players mid-run should do.
3. Title the pull request with the break and reference the release it lands
   in. A release that carries a break changes V, R, or M, never only F.

If you are not sure whether a change is breaking, treat it as breaking and
ask in the pull request before merging.

## Checks to run before opening a client PR

- `cargo fmt -- --check` (CI fails on formatting alone, with `-Dwarnings`).
- `cargo test -p bb-archipelago --lib -- --test-threads=1` for Bloodborne;
  one logging test is flaky under parallel execution.
- Add or update a test that loads state written by the previous release's
  shape whenever you touch persistence.
